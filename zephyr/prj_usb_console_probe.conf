# Diagnostic only: retain the Zephyr console initialization path selected by
# the upstream CDC ACM sample. The head protocol remains binary; this file is
# not a production logging configuration.
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_STDOUT_CONSOLE=y
